server:
  port: 8002
spring:
  profiles:
    active: dev
  application:
    name: dubbo-springboot-demo-consumer
dubbo:
  application:
    name: ${spring.application.name}
  protocol:
    name: dubbo
  reference:
    url: dubbo://127.0.0.1:20880  # 直连服务提供方，不使用注册中心
